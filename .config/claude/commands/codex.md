---
description: Send a prompt to Codex GPT-5.2-Codex and add output to context
argument-hint: <prompt for codex>
---

# Codex Query

Run the following prompt through Codex GPT-5.2-Codex and include the full output in your response:

**Prompt:** $ARGUMENTS

Use the Bash tool to execute: `codex exec -m gpt-5.2-codex` with the prompt above (properly escaped for bash).

After receiving the output, present it clearly so we can discuss or work with the results.
